
<div class="col s6">
	{{#each burgers}}
	{{#if this.devoured}}
	<div>
		<form action="/burgers/{{this.id}}?_method=DELETE" method="POST">
			<button type="submit" class="delete-burger button-delete-burger" data-id="{{id}}"
				data-newDevour="{{devour}}">
				{{this.burger_name}}
			</button>
		</form>
	</div>
	{{/if}}
	{{/each}}
</div>