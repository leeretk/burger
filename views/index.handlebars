
<div class="wrapper">
      <h1 class="text-center">Eat-Da-Burger!</h1>
      <h2 class="text-center">Everybody Wants Some!</h2>
      <div class="videowrapper">
              <iframe width="1280" height="720" src="https://www.youtube.com/embed/CxZy3Tr2wQM" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>

              <iframe  src="https://i.gifer.com/TF5O.mp4" ></iframe>

              <iframe  src="https://i.gifer.com/7dS3.mp4" ></iframe>
</div>
              <img src="assets/img/betterOffDeadBurger.JPG">
              <img src="assets/img/burgers1.JPG">
              <br>
 <p>
            {{this.burger_name}}
          </p>
<main>

      <div class="row">
            <div>
      </div>
  <div class="container">
      <div class="col s6">
        {{#each burgers}}
          {{#unless this.devoured}}
            <div>
              <form action="/burgers/{{this.id}}?_method=PUT" method="POST">
                <button type="submit" class='burgerAvailable'> {{this.burger_name}}</button>
              </form>
            </div>
          {{/unless}}
        {{/each}}
      </div>
      <div class="col s6">
        {{#each burgers}}
          {{#if this.devoured}}
            <div class='burgerEaten'>
              <div>
                {{this.burger_name}}
              </div>
            </div>
          {{/if}}
        {{/each}}
      </div>
    </div>
  </div>
</main>

<h2>Add A Burger Option</h2>

<div class="row">
  <div class="col s12">
    <form action="/burgers" method="POST">
      <input type="text" id="name" name="burger_name" placeholder="I want some too!">
      <button type="submit" class="addBurgerButton">Add The Burger</button>
    </form>
  </div>
</div>
</div>